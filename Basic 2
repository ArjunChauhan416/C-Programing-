Write a C program to find maximum between three number?

#include <stdio.h>

int main()
{
 int a,b,c;
 scanf("%d%d%d",&a,&b,&c);
 if(a>b || a>c)
 printf("%d",a);
 else if(b>a || b>c)
 printf("%d",b);
 else
 printf("%d",c);

    return 0;
}
------------------------------------------------------------------------------------------------------
Write a C program to find maximum between two number?

include <stdio.h>

int main()
{
 int a,b;
 scanf("%d%d",&a,&b);
 if(a>b)
 printf("%d",a);
 else
 printf("%d",b);

    return 0;
}
------------------------------------------------------------------------------------------------------
Write a C program to check whether a number is negative , positive or zero?


#include <stdio.h>

int main()
{
 int a;
 scanf("%d",&a);
 if(a>0)
 printf("Positive");
 else if(a<0)
 printf("Negative");
 else
 printf("Zero");
 

    return 0;
}
------------------------------------------------------------------------------------------------------

Write a c program to check whether a number is divisible by 5 and 11 or not?

#include <stdio.h>

int main()
{
 int a;
 scanf("%d",&a);
 if(a%5==0 && a%11==0)
 printf("YES ");
 else
 printf("NO ");
     return 0;
}
------------------------------------------------------------------------------------------------------
Write a C Program to Check Wether a number is even or odd?

#include <stdio.h>

int main()
{
 int a;
 scanf("%d",&a);
 if(a%2==0)
 printf("Even ");
 else
 printf("Odd");
return 0;
}
------------------------------------------------------------------------------------------------------
Write a C program to check wether a year is leap yera or not?

#include <stdio.h>

int main() {
    int ye;
    scanf("%d", &ye);
    
    if (ye % 4 == 0) 
    {
        if (ye % 100 == 0) 
        {
            if (ye % 400 == 0)
                printf("%d is a leap year.\n", ye);
            else
                printf("%d is not a leap year.\n", ye);
        } 
        else
            printf("%d is a leap year.\n", ye);
    } 
    else
        printf("%d is not a leap year.\n", ye);
    
    return 0;
}

------------------------------------------------------------------------------------------------------
Write a C program To check whether a character is alphabet or not?

#include <stdio.h>

void main()
{
char ch;
scanf("%c",&ch);
if((ch>='a' && ch<='z') || (ch>='A' && ch<='Z'))
printf("Alphabet");
else 
printf("Not a Alphabet ");
}
------------------------------------------------------------------------------------------------------
Write a C program To input any alphabet and check whether it is vowel or consonant?

void main()
{
char ch;
scanf("%c",&ch);
if((ch>='a' && ch<='z') || (ch>='A' && ch<='Z'))
{
    if(ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u' || ch=='A' || ch=='E' || ch=='I' || ch=='O' || ch=='U')
    printf("Vowel");
    else 
    printf("Consonant");
    }
    else
    printf("Not a alphabet ");
}
------------------------------------------------------------------------------------------------------
Write a C program to input any character and check whether it is alphabet, digit or special character?

#include <stdio.h>

void main()
{
char ch;
scanf("%c",&ch);
if((ch>='a' && ch<='z') || (ch>='A' && ch<='Z'))
{
    if(ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u' || ch=='A' || ch=='E' || ch=='I' || ch=='O' || ch=='U')
    printf("Vowel");
    else 
    printf("Consonant");
    }
    else if(ch>='0' && ch<='9')
    printf("Digit ");
    else 
    printf("Special Character ");
}
------------------------------------------------------------------------------------------------------
Write a program to find maximum and minimum between two numbers?

#include <stdio.h>

int main()
{
 int a,b;
 scanf("%d%d",&a,&b);
 if(a>b)
 printf("%d",a);
 else
 printf("%d",b);

    return 0;
}
