Write a C program to read and print elements of array?

#include <stdio.h>
int main()
{
    int a[5];
    printf("Input any 5 elements in array \n");
    for(int i=0;i<5;i++)
    {
    scanf("%d",&a[i]);    
    }
    printf("input array is");
    for(int i=0;i<5;i++)
    {
    printf("Input array is %d\n",a[i]);    
    }
    return 0;
}
-----------------------------------------------------------------------------------------------------------
Write a C program to print all negative elements in an array?

#include <stdio.h>
int main()
{
    int a[5],i=0;
    printf("Input any 5 elements in array \n");
    for(i=0;i<5;i++)
    {
    scanf("%d",&a[i]);    
    }
    printf("Negative elements in array:\n ");
    for(i=0;i<5;i++)
    {
        if(a[i]<0)
        printf("%d\n",a[i]);
    }
    return 0;
}
-----------------------------------------------------------------------------------------------------------
Write a C program to find sum of all array elements ?

#include <stdio.h>
int main()
{
    int a[5],i=0,s=0;
    printf("Input any 5 elements in array \n");
    for(i=0;i<5;i++)
    {
    scanf("%d",&a[i]);
    s=s+a[i];
    }
    printf("Sum of all array :%d\n ",s);
    return 0;
}
-----------------------------------------------------------------------------------------------------------
Write a C program to find maximu and minimum element in an array?
#include <stdio.h>
int main()
{
    int a[5],i=0,max=0,min=0;
    printf("Input any 5 elements in array \n");
    for(i=0;i<5;i++)
    {
    scanf("%d",&a[i]);
    }
    min=a[0];
    max=a[0];
    for(i=0;i<5;i++)
    {
        if(a[i]>max)
        max=a[i];
        if(a[i]<min)
        min=a[i];
        
    }
    printf("Maximum: %d\n",max);
    printf("Minimum: %d\n",min);
    return 0;
}
-----------------------------------------------------------------------------------------------------------
Write a C program to find second largest element in an array?
